spring:
  datasource:
    url: jdbc:mysql://localhost:3306/casse_automatiche
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10 
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true  
    properties:
      hibernate:
        format-sql: true
        
jwt:
  secret-key: "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa" 
  expiration: 360000  # Durata in millisecondi, un ora per fare test